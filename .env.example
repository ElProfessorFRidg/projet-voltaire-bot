# Exemple de configuration pour Projet Voltaire JS 2.0
# Copiez ce fichier en .env et adaptez les valeurs selon votre environnement.

# --- Authentification Projet Voltaire ---
# Les comptes sont désormais gérés dans le fichier config/accounts_config.json
# Voir la documentation dans le README pour le format attendu.

# --- URL de connexion Projet Voltaire ---
VOLTAIRE_LOGIN_URL=https://www.projet-voltaire.fr/voltaire/com.woonoz.gwt.woonoz.Voltaire/Voltaire.html?module=ORTHOGRAPHE
# URL de la page de connexion Projet Voltaire (ne modifiez que si nécessaire).

# --- Clé API OpenAI ---
OPENAI_API_KEY=
# Clé d'API OpenAI obligatoire pour la résolution automatique des exercices.

OPENAI_MODEL=gpt-4.1-mini
# Modèle OpenAI à utiliser (ex : gpt-4.1-mini, gpt-4o-mini, gpt-3.5-turbo, etc.)

# --- Paramètres de simulation humaine (en millisecondes) ---
MIN_ACTION_DELAY=500
# Délai minimum entre deux actions automatisées (ms).

MAX_ACTION_DELAY=1500
# Délai maximum entre deux actions automatisées (ms).

MIN_TYPING_DELAY=50
# Délai minimum entre deux frappes clavier simulées (ms).

MAX_TYPING_DELAY=150
# Délai maximum entre deux frappes clavier simulées (ms).

# --- Niveau de log ---
LOG_LEVEL=info
# Niveau de verbosité des logs (error, warn, info, verbose, debug, silly).

# --- Variables avancées (optionnelles) ---
# BROWSER_TYPE=chromium
# Type de navigateur Playwright à utiliser (chromium, firefox, webkit). À implémenter si besoin.
# HEADLESS_MODE=true
# Mode sans interface graphique (true pour invisible, false pour visible). À implémenter si besoin.